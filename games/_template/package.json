{
  "name": "mygame",
  "private": true,
  "scripts": {
    "build": "esbuild scorer.ts --bundle --platform=node --target=es2017 --outfile=scorer.js",
    "serve": "bgb serve ."
  },
  "dependencies": {
    "@boardgamebuddy/game-pack-api": "^0.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}