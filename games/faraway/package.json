{
  "scripts": {
    "build": "esbuild scorer.ts --bundle --platform=node --target=es2017 --outfile=scorer.js",
    "test": "npx jest --config ../../jest.config.js --testPathPattern=faraway",
    "serve": "node ../serve.js"
  }
}
